==> Authentication
  ==> register (1.to save user data , 2. to provide token to the user)
  ==> login
  ==> logout(token blacklisting)
  ==> [OTP Based registration]

==> Post
  ==> create
  ==> can see the feed
  ==> like posts (collection types)
  ==> save posts

==> users
  ==> follow
  ==> follower/following
    


    post Schema = {
         caption: String,
         img_url : String,
         user:userId,
         createdAt:Date-time
    }

    user Schema = {
      username:String, unique , required
      password:string, required
      profileImage:String,
      email:String , unique , required
      bio:string
    }